# Discord user token (required for deleting your own messages).
# Never commit .env or share your token.
# Get it from DevTools → Application → Local Storage → discord.com → token (use at your own risk).
DISCORD_TOKEN="token"
